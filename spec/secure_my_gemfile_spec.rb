require 'spec_helper'

describe SecureMyGemfile do
  it 'has a version number' do
    expect(SecureMyGemfile::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
